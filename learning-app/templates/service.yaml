apiVersion: v1
kind: Service
metadata:
  name: {{ include "learning-app.fullname" . }}
  labels:
    {{- include "learning-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "learning-app.selectorLabels" . | nindent 4 }}
